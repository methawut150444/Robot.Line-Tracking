void trackingLine(){
  readSensor();
  if(sv2 == 0 && sv3 == 1) motor(0, 255); // หุ่นเบ้ขวา -> เลี้ยวซ้าย (ล้อซ้ายหยุด ล้อขวาหมุน)
  if(sv2 == 1 && sv3 == 0) motor(255, 0); // หุ่นเบ้ซ้าย -> เลี้ยวขวา (ล้อซ้ายหมุน ล้อขวาหยุด)
}